<?php
	//全局可以使用的通用函数声明在这个文件中.
	function ip_to_long($Ip) { 
   		$array=explode('.',$Ip); 
   		$Int=($array[0] * 256*256*256) + ($array[1]*256*256) + ($array[2]*256) + $array[3]; 
   		return $Int; 
	} 